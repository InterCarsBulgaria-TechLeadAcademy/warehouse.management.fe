// Here we will put paths as constants so we can reuse them better inside the projects
// objects also work to encapsulate them better but use Object.freeze so it cant mutate the object

export const PROJECTS_PATH_EXAMPLE = '/projects'
export const LOGIN_PATH = '/login'
export const DEFAULTLAYOUT_PATH = '/defaultLayout'
export const USERS_PATH = '/users'
export const ZONES_PATH = 'zones'
export const VENDORS_PATH = 'vendors'
export const ROLES_PATH = 'roles'
export const TYPESGOODS_PATH = 'typesGoods'
export const TYPESDIFFERENCE_PATH = 'typesDifference'
export const MARKERS_PATH = 'markers'
